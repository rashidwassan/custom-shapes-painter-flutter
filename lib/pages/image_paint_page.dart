import 'package:flutter/material.dart';

class ImagePaintPage extends StatelessWidget {
  const ImagePaintPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
